# Anticipated Haskell Test Layout

Expected test focus:

- palette parsing
- fontset parsing
- normalized state codecs
- command parsing and validation
- `rc.xml` / `menu.xml` generation
- panel/subpanel layout generation
- migration behavior from legacy inputs

This scaffold exists to reserve a clear home for non-UI runtime validation.

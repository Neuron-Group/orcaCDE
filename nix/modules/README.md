# Anticipated Nix Modules

Reserved for declarative `NsCDE` configuration modules, for example:

- default style/profile selections
- session environment exports
- static menu/theme/template selection
- nested test profile assembly

These modules should describe defaults and assembly, not long-lived runtime
control flow.

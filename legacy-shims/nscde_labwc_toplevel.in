#!/bin/sh

#
# NsCDE foreign toplevel daemon wrapper for labwc backend.
#
# This script wraps the compiled nscde_toplevel binary.
#
# This file is a part of NsCDE - Not so Common Desktop Environment
# Author: Hegel3DReloaded
# Licence: GPLv3
#

if [ -z "${OS_PLUS_MACHINE_ARCH:-}" ]; then
    OS_PLUS_MACHINE_ARCH=$(uname -s)_$(uname -m)
fi

BIN="${NSCDE_TOOLSDIR}/${OS_PLUS_MACHINE_ARCH}/nscde_toplevel"

if [ ! -x "$BIN" ]; then
    echo "Error: nscde_toplevel binary not found at $BIN" >&2
    exit 1
fi

exec "$BIN" "$@"

#!/bin/sh

#
# Compatibility wrapper for the Haskell runtime daemon.
#

_runtime_bin="${NSCDE_RUNTIME_BIN:-nscde-runtime}"

if command -v "$_runtime_bin" >/dev/null 2>&1; then
   exec "$_runtime_bin" daemon "$@"
fi

echo "nscde_sessiond: runtime binary not found: $_runtime_bin" >&2
exit 127
